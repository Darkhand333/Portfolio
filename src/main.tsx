import React from 'react';
import ReactDOM from 'react-dom/client';
import OptimizedPortfolio from './pages/OptimizedPortfolio.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OptimizedPortfolio />
  </React.StrictMode>,
);